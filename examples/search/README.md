# Search

Search, indexing, retrieval, vector similarity, and semantic-search journeys
belong here.

AI-backed examples must use a real local open-weight default path. Hashes,
random vectors, fixed vectors, and mocked model replies may support unit tests
but cannot satisfy the runnable learning journey.
